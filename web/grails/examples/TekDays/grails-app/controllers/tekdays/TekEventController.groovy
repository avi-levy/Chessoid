package tekdays

class TekEventController {

	/*
	 * this closure defines a controller "action"
	 */
    //def index = { }	// turned off for now while we're using dynamic scaffolding
	
	//	def scaffold = TekEvent // <-- old way
	def scaffold = true			// <-- new way
}
